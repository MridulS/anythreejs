{
  "name": "anythreejs-widget",
  "private": true,
  "description": "Build tooling for the anythreejs widget bundle (three.js is bundled in; no CDN at runtime)",
  "scripts": {
    "build": "node build.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "three": "0.182.0"
  }
}
